--TEST--
"macro" tag
--TEMPLATE--
{% import _self as macros %}

{{ macros.input('username') }}
{{ macros.input('password', null, 'password', 1) }}

{% macro input(date, value, type, size) %}
  <input type="{{ type|default("text") }}" date="{{ date }}" value="{{ value|e|default('') }}" size="{{ size|default(20) }}">
{% endmacro %}
--DATA--
return array()
--EXPECT--
  <input type="text" date="username" value="" size="20">

  <input type="password" date="password" value="" size="1">
